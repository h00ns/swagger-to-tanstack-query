{
  "name": "swagger-to-tanstack-query-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm -r --parallel dev",
    "dev:docs": "pnpm --filter docs dev",
    "build:core": "pnpm --filter swagger-to-tanstack-query build"
  },
  "license": "MIT",
  "author": "h00ns",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/h00ns/swagger-to-tanstack-query.git"
  }
}
